[project]
name = "edemi-backend"
version = "0.1.0"
description = "Backend service and RAG runtime for Edemi."
readme = "readme.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "accelerate",
    "asyncpg",
    "bitsandbytes",
    "chromadb",
    "docx2txt",
    "fastapi",
    "FlagEmbedding",
    "hf-xet",
    "httpx[http2]",
    "huggingface-hub",
    "langchain",
    "langchain-classic",
    "langchain-community",
    "langchain-exa>=1.1.0",
    "langchain-milvus",
    "langchain-ollama",
    "langchain-openai",
    "langchain-qdrant",
    "langchain-text-splitters",
    "langgraph",
    "langgraph-checkpoint",
    "langgraph-checkpoint-postgres",
    "langgraph-prebuilt",
    "langgraph-sdk",
    "langmem",
    "langsmith",
    "numpy",
    "ollama",
    "onnxruntime",
    "openai",
    "pandas",
    "peft",
    "psycopg[binary,pool]",
    "pydantic",
    "pyjwt>=2.13.0",
    "pymilvus[model]",
    "pymupdf",
    "pymupdf4llm",
    "pypdf",
    "python-docx",
    "python-dotenv",
    "pyyaml",
    "qdrant-client<1.16.0",
    "redis",
    "requests",
    "scikit-learn",
    "scipy",
    "sqlmodel",
    "streamlit",
    "tiktoken",
    "torch",
    "tqdm",
    "transformers",
    "trl",
    "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git",
    "uvicorn[standard]",
    "websockets",
]

[project.optional-dependencies]
dev = [
    "deepeval",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/edemi_server", "src/rag"]

[tool.hatch.build.targets.sdist]
include = [
    "/pyproject.toml",
    "/readme.md",
    "/src/edemi_server",
    "/src/rag",
    "/src/streamlit_app.py",
]

[tool.uv]
package = true
prerelease = "disallow"
index-strategy = "unsafe-best-match"

[[tool.uv.index]]
name = "pytorch-nightly-cu128"
url = "https://download.pytorch.org/whl/nightly/cu128"
